/* ==========================================================================
   gallery.js — Gallery interactions.

   Currently a stub. Used on service gallery pages and the future
   /projects/index.html. Two features planned:

   1. LIGHTBOX — click any .cell img, opens fullscreen with prev/next.
   2. FILTER  — buttons that show/hide cells by data-category attribute.

   Both can be added without changing the HTML structure, since cells
   already carry the right hooks (.cell, .cap span for category).

   Build this out when the client has supplied real project photos
   and you're ready to ship the full Projects page.
   ========================================================================== */

(function(){
  'use strict';

  // --- Lightbox stub ---
  // const cells = document.querySelectorAll('.cell');
  // cells.forEach(cell => cell.addEventListener('click', openLightbox));
  //
  // function openLightbox(e){ ... }

  // --- Filter stub ---
  // const filterBtns = document.querySelectorAll('[data-filter]');
  // filterBtns.forEach(b => b.addEventListener('click', applyFilter));
  //
  // function applyFilter(e){ ... }

})();
